<ol reversed class="worklist">
{% for post in postslist | reverse %}
  <li class="worklist-item{% if post.url == url %} worklist-item-active{% endif %}">
    <a href="{{ post.url | url }}" class="worklist-link">{% if post.data.title %}{{ post.data.title }}{% else %}<code>{{ post.url }}</code>{% endif %}</a>
    <p class="worklist-details">{{ post.data.client }} / {{ post.data.year }}</p>
    {% for tag in post.data.tags | filterTagList %}
      {% set tagUrl %}/tags/{{ tag | slug }}/{% endset %}
      <a href="{{ tagUrl | url }}" class="post-tag">{{ tag }}</a>
    {% endfor %}
  </li>
{% endfor %}
</ol>
